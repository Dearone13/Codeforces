/**
   //Dearone
 
   created: 01/02/24 21:16:35
**/
#include <bits/stdc++.h>

#define ll  long long
#define br  '\n'

using namespace std;

int main() {

   //Complejidad 1 ->> Es la expresión algoritmica más simple la más rapida
   int n = 1 + 2; 
   //Complejidad O(n) -->> Es la expresión algoritmica de bucle o linea
   for (int  i = 0; i < n; i++)
   {
      cin >> i;
   }
   //Complejidad O(n)2 -->Es una aexpresión algoritmica de bucla anidado
   for(int i = 0;i <n ; i++){  // nxn = n2
      for (int j = 0; j < n; j++) {
      }
   }
   //Complejdiad algoritmica O(log[n])
   for (int i = 0; i < n; i /= c) {

   }
   for (int i = 0; i < n; i *= c)
   {
   }
   // Complejidad  O(log[n])(log[n])
   for (int i = 0; i < ; i++) {

   }

   // 3 * 10e11 --> int long  -- long long 


   ios::sync_with_stdio(false);
   cin.tie(0);
   
   return 0;
}